[workspace]
members = [
    "programs/aura-core",
    "programs/aura-policy",
    # "packages/sdk-rs",  # deferred: SDK rework pending until all program features land; re-enable then
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
version = "0.1.0"

[profile.release]
overflow-checks = true
