import type { ReactNode } from "react";

export const metadata = {
  title: "AURA — Colosseum Frontier 2026",
  description:
    "Encrypted spending guardrails for autonomous AI agent treasuries across every chain.",
};

export default function PitchDeckLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
