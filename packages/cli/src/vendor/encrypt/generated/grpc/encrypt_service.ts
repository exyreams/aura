// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: encrypt_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "encrypt.v1";

/** Chain identifier. */
export enum Chain {
  SOLANA = 0,
  UNRECOGNIZED = -1,
}

export function chainFromJSON(object: any): Chain {
  switch (object) {
    case 0:
    case "SOLANA":
      return Chain.SOLANA;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Chain.UNRECOGNIZED;
  }
}

export function chainToJSON(object: Chain): string {
  switch (object) {
    case Chain.SOLANA:
      return "SOLANA";
    case Chain.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** A single encrypted input value. */
export interface EncryptedInput {
  /** FHE ciphertext bytes (encrypted under the request's network_encryption_public_key). */
  ciphertextBytes: Buffer;
  /** FHE type discriminator (0-44). Validated server-side. */
  fheType: number;
}

export interface CreateInputRequest {
  /** Target chain where the ciphertexts should be created. */
  chain: Chain;
  /** One or more encrypted input values to create on-chain. */
  inputs: EncryptedInput[];
  /** ZK proof of valid encryption covering all inputs (skipped in dev mode). */
  proof: Buffer;
  /**
   * Who can use these ciphertexts (program/contract address or user address).
   * Shared across all inputs in this batch.
   */
  authorized: Buffer;
  /**
   * Network encryption public key all inputs were encrypted under.
   * 32 bytes. Must match an active key registered on-chain.
   */
  networkEncryptionPublicKey: Buffer;
}

export interface CreateInputResponse {
  /** On-chain ciphertext identifiers, one per input (same order as request). */
  ciphertextIdentifiers: Buffer[];
}

export interface ReadCiphertextRequest {
  /** BCS-serialized ReadCiphertextMessage (the signed payload). */
  message: Buffer;
  /** Ed25519 signature over `message` by the authorized party. */
  signature: Buffer;
  /**
   * Public key of the signer (32 bytes).
   * Must match the ciphertext's `authorized` field, or the ciphertext must be public.
   */
  signer: Buffer;
}

export interface ReadCiphertextResponse {
  /**
   * Production: ciphertext re-encrypted under the user's re-encryption key.
   *             The user decrypts locally with their private key.
   * Mock: raw plaintext bytes (fhe_type determines interpretation).
   */
  value: Buffer;
  /** FHE type of the ciphertext. */
  fheType: number;
  /** On-chain digest (for client-side verification). */
  digest: Buffer;
}

function createBaseEncryptedInput(): EncryptedInput {
  return { ciphertextBytes: Buffer.alloc(0), fheType: 0 };
}

export const EncryptedInput: MessageFns<EncryptedInput> = {
  encode(message: EncryptedInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ciphertextBytes.length !== 0) {
      writer.uint32(10).bytes(message.ciphertextBytes);
    }
    if (message.fheType !== 0) {
      writer.uint32(16).uint32(message.fheType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EncryptedInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEncryptedInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ciphertextBytes = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fheType = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EncryptedInput {
    return {
      ciphertextBytes: isSet(object.ciphertextBytes)
        ? Buffer.from(bytesFromBase64(object.ciphertextBytes))
        : isSet(object.ciphertext_bytes)
        ? Buffer.from(bytesFromBase64(object.ciphertext_bytes))
        : Buffer.alloc(0),
      fheType: isSet(object.fheType)
        ? globalThis.Number(object.fheType)
        : isSet(object.fhe_type)
        ? globalThis.Number(object.fhe_type)
        : 0,
    };
  },

  toJSON(message: EncryptedInput): unknown {
    const obj: any = {};
    if (message.ciphertextBytes.length !== 0) {
      obj.ciphertextBytes = base64FromBytes(message.ciphertextBytes);
    }
    if (message.fheType !== 0) {
      obj.fheType = Math.round(message.fheType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EncryptedInput>, I>>(base?: I): EncryptedInput {
    return EncryptedInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EncryptedInput>, I>>(object: I): EncryptedInput {
    const message = createBaseEncryptedInput();
    message.ciphertextBytes = object.ciphertextBytes ?? Buffer.alloc(0);
    message.fheType = object.fheType ?? 0;
    return message;
  },
};

function createBaseCreateInputRequest(): CreateInputRequest {
  return {
    chain: 0,
    inputs: [],
    proof: Buffer.alloc(0),
    authorized: Buffer.alloc(0),
    networkEncryptionPublicKey: Buffer.alloc(0),
  };
}

export const CreateInputRequest: MessageFns<CreateInputRequest> = {
  encode(message: CreateInputRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chain !== 0) {
      writer.uint32(8).int32(message.chain);
    }
    for (const v of message.inputs) {
      EncryptedInput.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.proof.length !== 0) {
      writer.uint32(26).bytes(message.proof);
    }
    if (message.authorized.length !== 0) {
      writer.uint32(34).bytes(message.authorized);
    }
    if (message.networkEncryptionPublicKey.length !== 0) {
      writer.uint32(42).bytes(message.networkEncryptionPublicKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateInputRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateInputRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.chain = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.inputs.push(EncryptedInput.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.proof = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.authorized = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.networkEncryptionPublicKey = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateInputRequest {
    return {
      chain: isSet(object.chain) ? chainFromJSON(object.chain) : 0,
      inputs: globalThis.Array.isArray(object?.inputs) ? object.inputs.map((e: any) => EncryptedInput.fromJSON(e)) : [],
      proof: isSet(object.proof) ? Buffer.from(bytesFromBase64(object.proof)) : Buffer.alloc(0),
      authorized: isSet(object.authorized) ? Buffer.from(bytesFromBase64(object.authorized)) : Buffer.alloc(0),
      networkEncryptionPublicKey: isSet(object.networkEncryptionPublicKey)
        ? Buffer.from(bytesFromBase64(object.networkEncryptionPublicKey))
        : isSet(object.network_encryption_public_key)
        ? Buffer.from(bytesFromBase64(object.network_encryption_public_key))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: CreateInputRequest): unknown {
    const obj: any = {};
    if (message.chain !== 0) {
      obj.chain = chainToJSON(message.chain);
    }
    if (message.inputs?.length) {
      obj.inputs = message.inputs.map((e) => EncryptedInput.toJSON(e));
    }
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    if (message.authorized.length !== 0) {
      obj.authorized = base64FromBytes(message.authorized);
    }
    if (message.networkEncryptionPublicKey.length !== 0) {
      obj.networkEncryptionPublicKey = base64FromBytes(message.networkEncryptionPublicKey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateInputRequest>, I>>(base?: I): CreateInputRequest {
    return CreateInputRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateInputRequest>, I>>(object: I): CreateInputRequest {
    const message = createBaseCreateInputRequest();
    message.chain = object.chain ?? 0;
    message.inputs = object.inputs?.map((e) => EncryptedInput.fromPartial(e)) || [];
    message.proof = object.proof ?? Buffer.alloc(0);
    message.authorized = object.authorized ?? Buffer.alloc(0);
    message.networkEncryptionPublicKey = object.networkEncryptionPublicKey ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseCreateInputResponse(): CreateInputResponse {
  return { ciphertextIdentifiers: [] };
}

export const CreateInputResponse: MessageFns<CreateInputResponse> = {
  encode(message: CreateInputResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.ciphertextIdentifiers) {
      writer.uint32(10).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateInputResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateInputResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ciphertextIdentifiers.push(Buffer.from(reader.bytes()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateInputResponse {
    return {
      ciphertextIdentifiers: globalThis.Array.isArray(object?.ciphertextIdentifiers)
        ? object.ciphertextIdentifiers.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : globalThis.Array.isArray(object?.ciphertext_identifiers)
        ? object.ciphertext_identifiers.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
    };
  },

  toJSON(message: CreateInputResponse): unknown {
    const obj: any = {};
    if (message.ciphertextIdentifiers?.length) {
      obj.ciphertextIdentifiers = message.ciphertextIdentifiers.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateInputResponse>, I>>(base?: I): CreateInputResponse {
    return CreateInputResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateInputResponse>, I>>(object: I): CreateInputResponse {
    const message = createBaseCreateInputResponse();
    message.ciphertextIdentifiers = object.ciphertextIdentifiers?.map((e) => e) || [];
    return message;
  },
};

function createBaseReadCiphertextRequest(): ReadCiphertextRequest {
  return { message: Buffer.alloc(0), signature: Buffer.alloc(0), signer: Buffer.alloc(0) };
}

export const ReadCiphertextRequest: MessageFns<ReadCiphertextRequest> = {
  encode(message: ReadCiphertextRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message.length !== 0) {
      writer.uint32(10).bytes(message.message);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    if (message.signer.length !== 0) {
      writer.uint32(26).bytes(message.signer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadCiphertextRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadCiphertextRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signature = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.signer = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadCiphertextRequest {
    return {
      message: isSet(object.message) ? Buffer.from(bytesFromBase64(object.message)) : Buffer.alloc(0),
      signature: isSet(object.signature) ? Buffer.from(bytesFromBase64(object.signature)) : Buffer.alloc(0),
      signer: isSet(object.signer) ? Buffer.from(bytesFromBase64(object.signer)) : Buffer.alloc(0),
    };
  },

  toJSON(message: ReadCiphertextRequest): unknown {
    const obj: any = {};
    if (message.message.length !== 0) {
      obj.message = base64FromBytes(message.message);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.signer.length !== 0) {
      obj.signer = base64FromBytes(message.signer);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadCiphertextRequest>, I>>(base?: I): ReadCiphertextRequest {
    return ReadCiphertextRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadCiphertextRequest>, I>>(object: I): ReadCiphertextRequest {
    const message = createBaseReadCiphertextRequest();
    message.message = object.message ?? Buffer.alloc(0);
    message.signature = object.signature ?? Buffer.alloc(0);
    message.signer = object.signer ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseReadCiphertextResponse(): ReadCiphertextResponse {
  return { value: Buffer.alloc(0), fheType: 0, digest: Buffer.alloc(0) };
}

export const ReadCiphertextResponse: MessageFns<ReadCiphertextResponse> = {
  encode(message: ReadCiphertextResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value.length !== 0) {
      writer.uint32(10).bytes(message.value);
    }
    if (message.fheType !== 0) {
      writer.uint32(16).uint32(message.fheType);
    }
    if (message.digest.length !== 0) {
      writer.uint32(26).bytes(message.digest);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadCiphertextResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadCiphertextResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fheType = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.digest = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadCiphertextResponse {
    return {
      value: isSet(object.value) ? Buffer.from(bytesFromBase64(object.value)) : Buffer.alloc(0),
      fheType: isSet(object.fheType)
        ? globalThis.Number(object.fheType)
        : isSet(object.fhe_type)
        ? globalThis.Number(object.fhe_type)
        : 0,
      digest: isSet(object.digest) ? Buffer.from(bytesFromBase64(object.digest)) : Buffer.alloc(0),
    };
  },

  toJSON(message: ReadCiphertextResponse): unknown {
    const obj: any = {};
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    if (message.fheType !== 0) {
      obj.fheType = Math.round(message.fheType);
    }
    if (message.digest.length !== 0) {
      obj.digest = base64FromBytes(message.digest);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadCiphertextResponse>, I>>(base?: I): ReadCiphertextResponse {
    return ReadCiphertextResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadCiphertextResponse>, I>>(object: I): ReadCiphertextResponse {
    const message = createBaseReadCiphertextResponse();
    message.value = object.value ?? Buffer.alloc(0);
    message.fheType = object.fheType ?? 0;
    message.digest = object.digest ?? Buffer.alloc(0);
    return message;
  },
};

/**
 * Client-facing gRPC API for the Encrypt executor.
 *
 * Same API across all chains and environments (local dev, devnet, testnet, mainnet).
 */
export type EncryptServiceService = typeof EncryptServiceService;
export const EncryptServiceService = {
  /**
   * Submit one or more encrypted input ciphertexts.
   *
   * A single ZK proof covers all inputs in the batch. All inputs share
   * the same authorized party and network encryption key. The executor
   * validates the proof (mock in dev mode), creates each ciphertext on-chain,
   * and returns their identifiers ready for immediate use.
   */
  createInput: {
    path: "/encrypt.v1.EncryptService/CreateInput" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateInputRequest): Buffer => Buffer.from(CreateInputRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateInputRequest => CreateInputRequest.decode(value),
    responseSerialize: (value: CreateInputResponse): Buffer => Buffer.from(CreateInputResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateInputResponse => CreateInputResponse.decode(value),
  },
  /**
   * Read a ciphertext off-chain.
   *
   * The request contains a BCS-serialized ReadCiphertextMessage signed by
   * the authorized party (or anyone if the ciphertext is public).
   *
   * In production: returns the ciphertext re-encrypted under the user's
   * re-encryption key (the user decrypts locally with their private key).
   * In mock mode: returns the plaintext value directly.
   *
   * Responses are cached per (ciphertext_identifier, signer, epoch).
   * When the epoch advances, the cache is invalidated and old signatures
   * are rejected.
   */
  readCiphertext: {
    path: "/encrypt.v1.EncryptService/ReadCiphertext" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReadCiphertextRequest): Buffer =>
      Buffer.from(ReadCiphertextRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReadCiphertextRequest => ReadCiphertextRequest.decode(value),
    responseSerialize: (value: ReadCiphertextResponse): Buffer =>
      Buffer.from(ReadCiphertextResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReadCiphertextResponse => ReadCiphertextResponse.decode(value),
  },
} as const;

export interface EncryptServiceServer extends UntypedServiceImplementation {
  /**
   * Submit one or more encrypted input ciphertexts.
   *
   * A single ZK proof covers all inputs in the batch. All inputs share
   * the same authorized party and network encryption key. The executor
   * validates the proof (mock in dev mode), creates each ciphertext on-chain,
   * and returns their identifiers ready for immediate use.
   */
  createInput: handleUnaryCall<CreateInputRequest, CreateInputResponse>;
  /**
   * Read a ciphertext off-chain.
   *
   * The request contains a BCS-serialized ReadCiphertextMessage signed by
   * the authorized party (or anyone if the ciphertext is public).
   *
   * In production: returns the ciphertext re-encrypted under the user's
   * re-encryption key (the user decrypts locally with their private key).
   * In mock mode: returns the plaintext value directly.
   *
   * Responses are cached per (ciphertext_identifier, signer, epoch).
   * When the epoch advances, the cache is invalidated and old signatures
   * are rejected.
   */
  readCiphertext: handleUnaryCall<ReadCiphertextRequest, ReadCiphertextResponse>;
}

export interface EncryptServiceClient extends Client {
  /**
   * Submit one or more encrypted input ciphertexts.
   *
   * A single ZK proof covers all inputs in the batch. All inputs share
   * the same authorized party and network encryption key. The executor
   * validates the proof (mock in dev mode), creates each ciphertext on-chain,
   * and returns their identifiers ready for immediate use.
   */
  createInput(
    request: CreateInputRequest,
    callback: (error: ServiceError | null, response: CreateInputResponse) => void,
  ): ClientUnaryCall;
  createInput(
    request: CreateInputRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateInputResponse) => void,
  ): ClientUnaryCall;
  createInput(
    request: CreateInputRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateInputResponse) => void,
  ): ClientUnaryCall;
  /**
   * Read a ciphertext off-chain.
   *
   * The request contains a BCS-serialized ReadCiphertextMessage signed by
   * the authorized party (or anyone if the ciphertext is public).
   *
   * In production: returns the ciphertext re-encrypted under the user's
   * re-encryption key (the user decrypts locally with their private key).
   * In mock mode: returns the plaintext value directly.
   *
   * Responses are cached per (ciphertext_identifier, signer, epoch).
   * When the epoch advances, the cache is invalidated and old signatures
   * are rejected.
   */
  readCiphertext(
    request: ReadCiphertextRequest,
    callback: (error: ServiceError | null, response: ReadCiphertextResponse) => void,
  ): ClientUnaryCall;
  readCiphertext(
    request: ReadCiphertextRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ReadCiphertextResponse) => void,
  ): ClientUnaryCall;
  readCiphertext(
    request: ReadCiphertextRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ReadCiphertextResponse) => void,
  ): ClientUnaryCall;
}

export const EncryptServiceClient = makeGenericClientConstructor(
  EncryptServiceService,
  "encrypt.v1.EncryptService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): EncryptServiceClient;
  service: typeof EncryptServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
