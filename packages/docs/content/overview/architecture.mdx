---
title: Architecture
description: How aura-core, aura-policy, the Ika network, and the off-chain packages fit together.
icon: Blocks
---

import { Card, Cards } from 'fumadocs-ui/components/card';

## System Overview

```mermaid
graph TB
  subgraph OnChain ["On-chain (Solana)"]
    Core["aura-core"]
    Policy["aura-policy"]
    Core --> Policy
  end

  subgraph Ika ["Ika Network"]
    Encrypt["Ika Encrypt"]
    DWallet["Ika dWallet"]
  end

  subgraph OffChain ["Off-chain"]
    Web["packages/web"]
    CLI["@aura-protocol/cli"]
    SdkTs["@aura-protocol/sdk-ts"]
    SdkRs["packages/sdk-rs"]
    Backend["packages/backend"]
  end

  RPC(["RPC"])
  GRPC(["gRPC"])
  CPI(["CPI: propose_confidential_transaction\nexecute_pending → approve_message"])

  Core --> CPI
  CPI --> Encrypt
  CPI --> DWallet

  Backend --> GRPC
  GRPC --> Encrypt
  GRPC --> DWallet

  SdkTs --> RPC
  SdkRs --> RPC
  RPC --> Core

  CLI --> SdkTs
  Web --> SdkTs
  Web -->|"REST"| Backend
```

## Package Responsibilities

| Package | Language | Role |
|---|---|---|
| `programs/aura-core` | Rust / Anchor 1.0.0 | On-chain program — treasury state machine, instruction handlers, CPIs to Encrypt and dWallet |
| `programs/aura-policy` | Pure Rust (no Anchor) | Policy engine — 27 violation codes, `evaluate_transaction`, `evaluate_public_precheck`, `evaluate_batch` |
| `packages/sdk-rs` | Rust | Sync RPC client, account decoding, PDA derivation, typed instruction builders |
| `packages/sdk-ts` | TypeScript | Anchor IDL wrappers, `AuraClient`, `Aura` facade, PDA helpers, event types |
| `packages/backend` | TypeScript / Node | Confidential bridge, agent runtime, SIWS auth, gRPC wrappers for Encrypt + dWallet |
| `packages/cli` | TypeScript | Commander CLI with Ink dashboard for all treasury operations |
| `packages/web` | TypeScript / Next.js 16 | Browser dashboard — wallet adapter, Radix UI, TanStack Query |

## Request Lifecycle

### Public proposal (`propose_transaction`)

```mermaid
sequenceDiagram
  participant Agent as AI Agent
  participant Core as aura-core
  participant Policy as aura-policy

  Agent->>Core: propose_transaction(amount_usd, target_chain, recipient_or_contract)
  Core->>Policy: evaluate_transaction(config, state, context)
  Note over Policy: Runs 17 rules in order
  Policy-->>Core: PolicyDecision { approved, violation, next_state }

  alt approved
    Core->>Core: store PendingTransaction, emit ProposalSubmitted
    Core-->>Agent: tx confirmed
  else violation
    Core-->>Agent: error AuraErrorCode::violation
  end
```

### Confidential proposal (`propose_confidential_transaction`)

```mermaid
sequenceDiagram
  participant Agent as AI Agent
  participant Core as aura-core
  participant Policy as aura-policy
  participant Encrypt as Ika Encrypt

  Agent->>Core: propose_confidential_transaction(amount_ciphertext, ...)
  Core->>Policy: evaluate_public_precheck(config, state, context)
  Note over Policy: Skips per_tx and daily_limit — deferred to FHE

  alt public precheck fails
    Core-->>Agent: error (no FHE call made)
  else public precheck passes
    Core->>Encrypt: CPI — evaluate(daily_limit_ct, per_tx_limit_ct, spent_today_ct, amount_ct)
    Encrypt-->>Core: policy_output_ciphertext (encrypted ViolationCode)
    Core->>Core: store PendingTransaction with policy_output_ciphertext
  end
```

### Execution (`execute_pending` → `finalize_execution`)

```mermaid
sequenceDiagram
  participant Operator
  participant Core as aura-core
  participant Encrypt as Ika Encrypt
  participant DWallet as Ika dWallet

  Note over Operator,Core: Confidential path — request decryption first
  Operator->>Core: request_policy_decryption(request_account, ciphertext)
  Core->>Encrypt: CPI — request_decryption(policy_output_ciphertext)
  Encrypt-->>Core: async — network processes decryption

  Operator->>Core: confirm_policy_decryption(request_account)
  Core->>Core: read decrypted ViolationCode
  Note over Core: 0 = approved, non-zero = rejected

  alt approved
    Operator->>Core: execute_pending(dwallet, message_approval, ...)
    Core->>DWallet: CPI — approve_message(dwallet_id, message_digest)
    DWallet-->>Core: MessageApproval PDA populated
    Operator->>Core: finalize_execution(message_approval, ...)
    Core->>Core: verify signature, close PendingTransaction, update counters
    Core-->>Operator: ProposalFinalized event
  else rejected
    Core-->>Operator: ProposalRejected event
  end
```

## Account Model

All AURA accounts are PDAs. The treasury is the root; all other accounts are derived from it.

```mermaid
erDiagram
  TreasuryAccount {
    PublicKey owner
    string agent_id
    PublicKey ai_authority
    PolicyConfig policy_config
    bool paused
    u64 created_at
    u64 total_transactions
  }
  PendingTransaction {
    PublicKey treasury
    u64 amount_usd
    u8 target_chain
    u8 tx_type
    string recipient_or_contract
    ProposalStatus status
    u64 submitted_at
  }
  DwalletRecord {
    PublicKey treasury
    u8 chain
    string dwallet_id
    string address
    u64 balance_usd
  }
  OperatorRole {
    PublicKey treasury
    PublicKey operator
    u8 permissions_bitmap
    u64 expires_at
  }
  SessionKeyAccount {
    PublicKey treasury
    PublicKey session_key
    u64 expires_at
    u64 max_amount_usd
  }
  HealthScore {
    PublicKey treasury
    u8 score
    u64 computed_at
  }
  TreasuryAccount ||--o| PendingTransaction : "has active"
  TreasuryAccount ||--o{ DwalletRecord : "registers per chain"
  TreasuryAccount ||--o{ OperatorRole : "grants"
  TreasuryAccount ||--o{ SessionKeyAccount : "issues"
  TreasuryAccount ||--o| HealthScore : "tracks"
```

## PDA Seeds

All seeds mirror `programs/aura-core/src/constants.rs` and are exported from `packages/sdk-ts/src/constants.ts`.

| Account | Seeds | Derivation program |
|---|---|---|
| Treasury | `["treasury", owner, agentId]` | `aura-core` |
| dWallet CPI authority | `["__ika_cpi_authority"]` | `aura-core` |
| Encrypt CPI authority | `["__encrypt_cpi_authority"]` | `aura-core` |
| Encrypt event authority | `["__event_authority"]` | Ika Encrypt |
| MessageApproval | `["dwallet", curveCode+pubkey chunks, "message_approval", schemeCode, digest]` | Ika dWallet |
| Policy receipt | `["policy_receipt", treasury, proposalId_le64]` | `aura-core` |
| Policy simulation | `["policy_simulation", treasury, simulationId_le64]` | `aura-core` |
| Budget envelope | `["budget_envelope", treasury, envelopeId_le64]` | `aura-core` |
| Exposure group | `["exposure_group", authority, groupId_16bytes]` | `aura-core` |
| Operator role | `["operator_role", treasury, operator]` | `aura-core` |
| External liveness | `["external_liveness", treasury]` | `aura-core` |
| Policy attestation | `["policy_attestation", treasury, attester, policyVersion_le64]` | `aura-core` |
| Batch proposal | `["batch_proposal", treasury, batchId_le64]` | `aura-core` |
| Invariant report | `["invariant_report", treasury, reportId_le64]` | `aura-core` |

## Source of Truth

The on-chain program is the source of truth. SDKs wrap the generated IDL rather than redefining instructions by hand.

```mermaid
flowchart LR
  Program["aura-core"] -->|"anchor build"| IDL["sdk-ts/src/generated/"]
  IDL -->|"generate-idl"| Types["constants.ts"]
  Types --> SdkTs["AuraClient"]
  Types --> SdkRs["sdk-rs"]
```

After any program change: `anchor build` → `npm run generate-idl` (or `:win` on Windows) in `packages/sdk-ts` → update SDK wrappers.

<Cards>
  <Card title="Confidential Guardrails" description="FHE ciphertexts, CPI accounts, and the decryption flow." href="/overview/confidential-guardrails" />
  <Card title="dWallet Execution" description="MessageApproval PDA, curve codes, and co-signing." href="/overview/dwallet-execution" />
  <Card title="Policy Engine" description="All 27 ViolationCodes and evaluation order." href="/overview/policy-engine" />
  <Card title="TypeScript SDK" description="AuraClient and Aura facade." href="/sdk-ts" />
</Cards>
